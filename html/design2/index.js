import { header } from './header';
import { instanceslist } from './instanceslist';
import { render as hosttemplate } from './hostrow';
import { render as servicetemplate } from './servicerow';

export const design = {
  header,
  instanceslist,
  hosttemplate,
  servicetemplate
}
